package SalamiRuntime.Runtime;

/**
 * The runtime values, or the values that are handled during evaluation. Things like numbers, floats, strings, etc.
 */
public enum RuntimeType {
    NUMBER,
    FLOAT,
    VOID,
    STRING,
    BOOLEAN,
    SUBROUTINE,
    METHOD,
}
